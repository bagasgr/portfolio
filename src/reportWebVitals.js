// src/reportWebVitals.js

// Function to log or send web vitals data to an analytics service
export default function logWebVitals(metric) {
    console.log(metric);  // Log the metrics to the console or send it to an analytics endpoint
  }
  